import db from '../models/index.js';
import bcrypt from 'bcrypt';
import dotenv from 'dotenv';

dotenv.config();

const seedDatabase = async () => {
    try {
        console.log('Database Seeding is starting');
    } catch(error) {

    }
}